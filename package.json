{
  "scripts": {
    "start": "rm -rf docs/* && parcel src/index.html -d docs",
    "build": "rm -rf docs/* && parcel build src/index.html -d docs"
  },
  "dependencies": {
    "preact": "^8.2.7",
    "preact-compat": "^3.18.0",
    "tinycolor2": "^1.4.1"
  },
  "devDependencies": {
    "babel-preset-env": "^1.6.1",
    "babel-preset-preact": "^1.1.0",
    "node-sass": "^4.8.3",
    "parcel-bundler": "^1.7.0"
  }
}
